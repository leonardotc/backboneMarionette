const assert = require('assert')

describe('videos', () =>  {

  describe('createYoutubehChannel()', () => {
    it('should return a canonical structure', () => {
      assert.equal(-1, [1,2,3].indexOf(4));
    });
  })

})